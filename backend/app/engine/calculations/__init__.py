"""
Engine calculations subpackage.

Contains pure formula functions (F-01 through F-22). Each formula function
takes explicit Decimal arguments and returns a Decimal result. No function
accepts EngineInput or EngineConfig directly — the orchestrator extracts
values and passes them explicitly.

Architecture: ENGINE_ARCHITECTURE.md Part 2 — calculations sub-module.
"""
