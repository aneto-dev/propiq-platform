"""
Repository layer package.

Contains abstract repository interfaces (interfaces/) and their
SQLAlchemy implementations. The service layer depends only on the
interfaces — never on the implementations directly.

Architecture: REPOSITORY_ARCHITECTURE.md Part 1–5.
"""
