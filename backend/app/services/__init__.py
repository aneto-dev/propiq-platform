"""
PropIQ application service layer.

Services orchestrate repositories, the calculation engine, and domain logic.
They do not own persistence (repositories do) and do not contain calculation
formulas (the engine does).

Architecture: APPLICATION_SERVICE_ARCHITECTURE.md
"""
