# PropIQ — Repository Alignment Report

Generated by inspection of the working repository against IMPLEMENTATION_ROADMAP.md.

---

## 1. Repository State Summary

```
propiq/
├── .env.example              ✅
├── .gitignore                ✅
├── Makefile                  ✅ (present — see deviation note below)
├── README.md                 ✅
├── backend/
│   ├── .python-version       ✅
│   ├── alembic.ini           ✅
│   ├── pyproject.toml        ✅
│   ├── alembic/
│   │   ├── env.py            ✅
│   │   ├── script.py.mako    ✅
│   │   └── versions/         ✅ (empty — correct at this stage)
│   ├── app/
│   │   ├── __init__.py       ✅
│   │   ├── main.py           ✅
│   │   ├── api/v1/router.py  ✅
│   │   ├── api/v1/routes/health.py  ✅
│   │   ├── core/config.py    ✅
│   │   ├── core/logging.py   ✅
│   │   ├── db/__init__.py    ✅
│   │   ├── db/base.py        ✅
│   │   └── db/session.py     ✅
│   └── tests/conftest.py     ✅
├── infrastructure/
│   ├── docker-compose.yml        ✅ (PostGIS 3.4 verified)
│   └── docker-compose.test.yml   ✅
├── scripts/
│   └── setup_dev.sh          ✅ (present — see deviation note below)
└── frontend/
    └── .gitkeep              ✅ (placeholder, correct — Phase 7)
```

**Not yet present (expected — future phases):**
- `app/domain/` — Commit 1.1+
- `app/engine/` — Commit 2.1+
- `app/repositories/` — Commit 4.1+
- `app/services/` — Commit 5.1+
- `app/db/models/` — Commit 3.1+
- `alembic/versions/*.py` — Commit 3.2+
- `frontend/` (beyond placeholder) — Commit 7.1+

---

## 2. Completed Commits

### ✅ Commit 0.1 — Monorepo skeleton
**Roadmap:** `chore: initialise monorepo structure`
**Verified:** .gitignore, .env.example, README.md, Makefile (stub),
top-level directories with .gitkeep placeholders.
**Status:** Complete and correct.

---

### ✅ Commit 0.2 — Docker Compose
**Roadmap:** `infra: add docker-compose for local postgres with PostGIS`
**Verified:** `infrastructure/docker-compose.yml` (postgis/postgis:16-3.4,
port 5432, pgAdmin on 5050) and `infrastructure/docker-compose.test.yml`
(port 5433, no volume). User confirmed: PostGIS 3.4 verified via
`SELECT PostGIS_Version()`.
**Status:** Complete and correct.

---

### ✅ Commit 0.3 — Python project skeleton
**Roadmap:** `chore: python 3.13 project with pyproject.toml`
**Verified:** `backend/pyproject.toml` with all required dependencies
(fastapi, sqlalchemy[asyncio], asyncpg, alembic, pydantic v2,
pydantic-settings, python-jose[cryptography], httpx, structlog, pytest,
pytest-asyncio, ruff, mypy). `backend/.python-version` = "3.13".
`asyncio_mode = "auto"` confirmed in pytest config.
User confirmed: poetry install successful, pytest runs (0 tests), ruff
and mypy pass.
**Note:** `backend/app/__init__.py` was added as a fix during this commit
to allow Poetry to recognise the package. This is correct and necessary.
**Status:** Complete and correct.

---

### ✅ Commit 0.4 — FastAPI application skeleton
**Roadmap:** `feat: fastapi application factory with health endpoint`
**Verified:**
- `app/main.py` — application factory pattern, lifespan handler, v1_router
  registered at `/api/v1`
- `app/core/config.py` — Pydantic BaseSettings with all required fields
  (database_url, supabase_*, environment, log_level, app_version)
- `app/core/logging.py` — structlog configured with JSON/console renderer
  split, base fields (_add_base_fields processor), stdlib bridge
- `app/api/v1/router.py` — v1_router aggregator
- `app/api/v1/routes/health.py` — GET /api/v1/health, no auth
- `tests/conftest.py` — intentionally minimal stub
**Status:** Complete and correct.

---

### ✅ Commit 0.5 — Alembic infrastructure
**Roadmap:** `chore: initialise alembic with async engine`
**Verified:**
- `alembic.ini` — script_location=alembic, prepend_sys_path=., UTC timezone,
  sqlalchemy.url as placeholder (overridden by env.py at runtime)
- `alembic/env.py` — full async implementation with NullPool,
  run_sync bridge, DATABASE_URL read from Settings, target_metadata=Base.metadata,
  compare_type=True, compare_server_default=True
- `alembic/script.py.mako` — migration template with downgrade no-op note
- `alembic/versions/.gitkeep` — correct empty state
- `app/db/base.py` — DeclarativeBase with no models yet
- `app/db/session.py` — async engine with pool_pre_ping=True,
  AsyncSessionLocal with expire_on_commit=False, get_db() FastAPI dependency
User confirmed: `poetry run alembic current` works, async migration
configuration verified.
**Status:** Complete and correct.

---

## 3. Partially Complete / Deviation Commits

### ⚠️ Commit 0.6 — Makefile targets and setup script

**Roadmap:** `chore: makefile targets for dev workflow`

**Roadmap specifies:**
- Updated Makefile with all targets (dev, test, migrate, seed, lint,
  typecheck, shell, etc.)
- `scripts/setup_dev.sh`

**Current state:**
- `Makefile` — EXISTS and is fully implemented with all required targets
  (dev, dev-db, test, test-unit, test-int, migrate, migrate-down,
  migrate-history, migrate-status, seed, lint, lint-fix, typecheck,
  format, shell, clean, help). The `dev` target correctly starts postgres
  then uvicorn.
- `scripts/setup_dev.sh` — EXISTS with full one-command setup script.

**Deviation:** These files were produced and committed during earlier
sessions as out-of-sequence work (before the Alembic commit was finalised,
during what the user correctly identified as roadmap drift). The user
identified this deviation and corrected course.

**Assessment:** The files exist and appear correct against the roadmap
specification. The content matches what Commit 0.6 should produce. The
deviation was in sequence (produced alongside or before Commit 0.5),
not in content.

**Recommendation:** If the developer has verified that the Makefile and
setup_dev.sh work correctly and match the roadmap specification, this commit
can be considered functionally complete. The gap is sequencing history in
git, not content correctness.

**If the developer's git history shows these files were committed as part
of a separate commit distinct from Commit 0.5, Commit 0.6 is complete.**

**If they were mixed into the same commit as Alembic work, the commit
boundary is slightly wrong but the content is correct.**

---

## 4. Phase 0 Exit Criteria Assessment

| Criterion | Status |
|---|---|
| `make dev` starts postgres and FastAPI on port 8000 | ✅ Makefile dev target verified |
| `GET /api/v1/health` returns 200 | ✅ Route exists and correct |
| `make test` runs (zero tests, all pass) | ✅ User confirmed |
| `make migrate` runs without error | ✅ Alembic confirmed working |
| Git log is clean with 6 focused commits | ⚠️ Depends on developer's actual git history |

Phase 0 is **functionally complete**. Whether the git history is clean
is the developer's own assessment.

---

## 5. Roadmap Deviations

### Deviation 1 — Commit sequence (minor)

**What:** The Makefile and setup_dev.sh from Commit 0.6 were produced at
the same time as or before the Alembic work from Commit 0.5, rather than
strictly after.

**Impact:** None on code correctness. Minor impact on git history cleanliness.

**Resolution:** If both files are in separate commits from the Alembic work,
the sequence is correct. If mixed, the deviation is in history only.

### Deviation 2 — setup_dev.sh not referenced in Makefile (minor)

**What:** The `scripts/setup_dev.sh` file exists but the Makefile does not
have a `make setup` or equivalent target that calls it. The roadmap does not
explicitly specify this, so this is not a technical deviation — but it may
reduce discoverability.

**Impact:** Minimal. The script can still be run directly.

**Resolution:** No action required unless the developer wishes to add a
convenience target.

### Deviation 3 — No domain/, engine/, or test subdirectories yet

**What:** `app/domain/`, `app/engine/`, `app/repositories/`, `app/services/`,
`app/db/models/`, and `tests/unit/`, `tests/regression/` directories do not
exist.

**Impact:** None — these are correctly deferred to Phase 1 and Phase 2 of
the roadmap. This is expected behaviour, not a deviation.

---

## 6. Out-of-Scope Files Confirmed Absent

The following were explicitly called out as out-of-scope additions in the
earlier session. They are confirmed absent from the repository:

| File | Status |
|---|---|
| Any business entity models | ✅ Absent |
| Any repository implementations | ✅ Absent |
| Any service implementations | ✅ Absent |
| Any ORM model files | ✅ Absent |
| Any migration version files | ✅ Absent (only .gitkeep) |
| Any seed scripts | ✅ Absent |
| Any frontend code | ✅ Absent (only .gitkeep) |

---

## 7. Next Commit

**Next commit to execute: Commit 0.6 (if not already committed separately)**
or **Commit 1.1 (if Commit 0.6 is confirmed complete in git).**

The developer should verify their git log:

```bash
git log --oneline
```

Expected commits at Phase 0 completion:
```
<hash> chore: makefile targets for dev workflow         ← 0.6
<hash> chore: initialise alembic with async engine      ← 0.5
<hash> feat: fastapi application factory with health endpoint  ← 0.4
<hash> chore: python 3.13 project with pyproject.toml   ← 0.3
<hash> infra: add docker-compose for local postgres with PostGIS ← 0.2
<hash> chore: initialise monorepo structure             ← 0.1
```

**If the git log shows these 6 commits cleanly, the next commit is 1.1.**

**Next commit: 1.1 — `feat(domain): define all domain enums`**

This begins Phase 1 — Domain Entities. It is a pure Python commit with no
database, no framework, and no infrastructure. It creates `app/domain/enums.py`
with all enum definitions used across the domain, engine, and persistence layers.

---

## 8. No Architecture Drift Detected

A review of all files present confirms:
- No calculation logic in any application file
- No ORM models in the engine layer
- No framework imports in any file that should be infrastructure-free
- All imports trace correctly to the layer boundaries defined in
  SERVICE_ARCHITECTURE.md and ENGINE_ARCHITECTURE.md
- Database URL is read from Settings in all database-touching files;
  never hardcoded

The implementation to date is architecturally aligned with all frozen
architecture documents.
